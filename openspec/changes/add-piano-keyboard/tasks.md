# Tasks

## 1. Подготовка

- [x] 1.1 Добавить `vitest` в devDependencies и скрипт `"test": "vitest run"` (D11); проверка: `npm test` запускается и сообщает «no test files» без ошибок, `npm run build` не сломан
- [x] 1.2 Обновить план этапов в CLAUDE.md, раздел 10: этап 2 — «Экранная клавиатура», прежний пункт 2 (модуль `midi/`) слить с этапом 1 как выполненный, нумерация остальных сохраняет порядок; проверка: в разделе 10 нет дубля пункта про `midi/`, пункты идут без пропусков

## 2. `engine/keyboard`: уровни и состояние клавиш

- [x] 2.1 `src/engine/keyboard/types.ts`: `KeyInput`, `KeyboardState`, `TouchHold`, `PlayedNote`, `Level = 1 | 2 | 3` (D1, D2); проверка: `npm run build` без ошибок типов
- [x] 2.2 `levels.ts`: `levelFromVelocity`, `levelFromPressure` (D3) + тесты на границы 42/43, 84/85, давление 0, 0.5, ⅓, ⅔, 1; проверка: `npm test` зелёный
- [x] 2.3 `keyboardState.ts`: редьюсер `reduce(state, input)` и выводы `isPressed`, `levelOf`, `heldPitches` (D2) + тесты по сценариям требований «Нажатие и отпускание», «Касание — полноценная сыгранная нота», «Пианино главнее касания» (все 4 сценария), «Глиссандо» (вкл/выкл), аккорд тремя пальцами; проверка: `npm test` зелёный, у каждого теста в названии — имя сценария из спеки

## 3. `engine/keyboard`: геометрия, прокрутка, подсветка

- [x] 3.1 `layout.ts`: `computeLayout`, `initialStart`, `resize`, `shift`, `isBlocked`, видимый диапазон высот (D4) + тесты: 1280px/16px rem → кнопки есть и чёрная ≥ 56px; 5200px → все 88 без кнопок; удвоенный rem → меньше видимых клавиш; C4 в центре при старте; resize сохраняет центр и прижимается к A0; блокировка у A0/C8; проверка: `npm test` зелёный
- [x] 3.2 `hitTest(x, y, layout, start)` (D6) + тесты: верх чёрной клавиши → чёрная, низ под ней → белая, граница белых, крайние обрезанные чёрные не попадают; проверка: `npm test` зелёный
- [x] 3.3 `scrollHints(heldPitches, visibleRange)` (D5) + тесты по сценариям «Подсветка кнопок прокрутки»: одна скрытая, обе стороны, отпускание, прокрутка к клавише, нота ниже A0, видна целиком; проверка: `npm test` зелёный
- [x] 3.4 `repeatDelayMs(heldMs)` (D7) + тест последовательности 500, 400, 300, 200, 100, 100 мс; проверка: `npm test` зелёный

## 4. Хранение настроек

- [x] 4.1 `src/storage/settings.ts`: `loadSettings`/`saveSettings`, ключ `piaono.settings.v1`, значения по умолчанию при отсутствии и битых данных, `try/catch` (D8) + тесты с подменённым `localStorage` (включая бросающий исключение); проверка: `npm test` зелёный

## 5. `ui/keyboard`: отрисовка и ввод

- [x] 5.1 Компонент `Keyboard`: зона с измерением ширины (`ResizeObserver`) и rem, клавиши абсолютным позиционированием по `computeLayout`, классы `key--pressed`/`key--level-N`, цвета и полосы из D12, без переходов; проверка: headless Chromium 1280×800 — скриншот, C4 в центре, контраст границ и нажатой клавиши ≥ 7:1 (скрипт расчёта контраста, как в этапе 1)
- [x] 5.2 Ввод касанием: обработчики Pointer Events на зоне с `setPointerCapture`, `hitTest`, мультитач по `pointerId`, `touchMove` для глиссандо, `touch-action: none`, `overscroll-behavior: none`; проверка: headless-скрипт с CDP `Input.dispatchTouchEvent` — касание E4 нажимает и отпускает; три пальца дают аккорд; свайп не смещает клавиатуру; с глиссандо C4→E4 даёт три сыгранные ноты
- [x] 5.3 Кнопки прокрутки: ширина белой, высота зоны, стрелка ≥ 2rem, автоповтор по `repeatDelayMs`, блокированный вид у края, подсветка по `scrollHints` заливкой `--ink-soft`; проверка: headless-скрипт — короткое касание сдвигает на одну клавишу, удержание 3 с даёт ожидаемое число сдвигов (±1), у A0 кнопка отключена; подменённый MIDI note-on C2 подсвечивает левую

## 6. Встраивание в приложение

- [x] 6.1 `App`: состояние клавиатуры через `useReducer(reduce)`, MIDI `noteOn/noteOff` → `pianoDown/pianoUp`, `playedNote` любого источника → `lastNoteName`; журнал «Проверки пианино» получает только события MIDI; проверка: headless — касание клавиши при подменённом «на связи» показывает крупное имя, в журнале проверки записи нет
- [x] 6.2 `WaitingScreen`: сетка из четырёх рядов с зоной `max(25svh, 8rem)`, клавиатура во всех шести состояниях, крупное имя ноты только в «на связи», середина с `overflow-y: auto`; `html, body` без прокрутки (D9); проверка: скриншоты шести состояний на 1280×800 и 740×360 — клавиатура одного размера и на одном месте, нет прокрутки страницы, на 740×360 прокручивается только подсказка
- [x] 6.3 Переключатель «Глиссандо» на `CheckScreen` (≥ 56×56 px, текст «Глиссандо: вкл/выкл»), состояние из `loadSettings`/`saveSettings`, передаётся в редьюсер; проверка: headless — включить, перезагрузить страницу, переключатель включён и скольжение играет ноты
- [x] 6.4 `orientation: 'landscape'` в манифесте `vite.config.ts` (D10); проверка: `npm run build`, в `dist/manifest.webmanifest` есть `"orientation":"landscape"`; вкладка 390×844 (портрет) — интерфейс перестроен, клавиатура внизу с кнопками

## 7. Интеграция и проверка на железе

- [x] 7.1 `npm run build`, `npm run lint`, `npm test` без ошибок; временные headless-скрипты удалены
- [ ] 7.2 В описании PR — чек-лист для планшета с пианино: клавиатура видна без пианино и с ним; игра на пианино подсвечивает клавиши с тремя уровнями силы (тихо/средне/громко); аккорд; нажатие скрытой клавиши подсвечивает кнопку, прокрутка к ней гасит подсветку; удержание кнопки проходит до C8; касание при подключённом пианино показывает крупное имя; пианино держит — палец отпущен — клавиша нажата; глиссандо вкл/выкл на «Проверке пианино»; поворот установленного приложения (при необходимости переустановить); системный жест «назад» у кнопок прокрутки не мешает; отклик клавиши на глаз одновременен с нажатием
