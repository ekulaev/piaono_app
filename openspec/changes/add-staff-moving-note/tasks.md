# Tasks

## 1. Логика упражнения (`engine/staff`)

- [x] 1.1 `pickNote(random)` и диапазоны ключей (D2) + тесты «Диапазон» (1000 нот: только белые B1–D6, ключ по диапазону, оба ключа встречаются) и «Общая зона ключей»; проверка: `npm test` зелёный
- [x] 1.2 Автомат упражнения `start/stop/played/tick/progress` (D1) + тесты по сценариям «Старт», «Стоп во время паузы», «Нота не сыграна» (5000 и 500 мс по расписанию), «Верная нота», «Та же нота в другой октаве», «Аккорд с верной нотой», «Повторная ошибка продлевает подсветку», «Нажатие во время паузы»; проверка: `npm test` зелёный

## 2. Отрисовка (`ui/staff`)

- [x] 2.1 Добавить `vexflow` (вход `vexflow/bravura`), лимит Workbox 5 МиБ и `chunkSizeWarningLimit` (D5); проверка: `npm run build`, размер чанка VexFlow записан в описание PR, чанк есть в precache-манифесте `dist/sw.js`
- [x] 2.2 Компонент стана: слой стана по размеру зоны (D3, D4), толстые линии, ожидание шрифта; проверка: headless-скриншоты 1280×800 и 740×360 — стан 90 % ширины, помещается целиком, нет прокрутки
- [x] 2.3 Слой ноты: ключ, целая нота с добавочными линиями, кольца верной/неверной ноты, токены цветов в `index.css`; движение через `transform` в `requestAnimationFrame` без React-рендера в кадре (D3); проверка: headless-скриншоты C4 и D6 в скрипичном, B1 в басовом ключе; в оттенках серого кольца различимы; контраст цветов ≥ 7:1 (скрипт)

## 3. Встраивание

- [x] 3.1 `useExercise` в `App`: сыгранные ноты → `played`, стоп при уходе на «Проверку пианино», удалить `lastNoteName` (D6); проверка: `npm run build`
- [x] 3.2 `WaitingScreen`: раскладка индикатор / подсказка / стан / кнопки / клавиатура, «Старт/Стоп» справа от «Проверки пианино», приглашение «Нажми „Старт“» (D6); проверка: headless — шесть состояний связи на 1280×800 и 740×360 без прокрутки страницы, клавиатура на месте
- [x] 3.3 Обновить CLAUDE.md §10, этап 3: нотный стан с движущейся нотой (C-STF-1, `add-staff-moving-note`), отличие от «без таймера» §7; проверка: текст раздела непротиворечив

## 4. Интеграция

- [x] 4.1 Headless-проверка упражнения (D7): старт → нота сразу; замер проезда 5 с ±100 мс и паузы 0,5 с ±50 мс; верное касание — остановка и сплошное кольцо; неверное — пунктир, движение продолжается; «Проверка пианино» останавливает; офлайн после первой загрузки ключ рисуется Bravura; e2e клавиатуры и выбора устройства прошлых этапов проходят (с поправкой на удалённое крупное имя ноты)
- [ ] 4.2 `npm run build`, `npm run lint`, `npm test` без ошибок; в описании PR — чек-лист для планшета: Старт/Стоп; нота видна крупно и читается; верная нота с пианино — зелёная со сплошным кольцом; неверная — красная с пунктиром; нота доезжает до ключа и исчезает; пауза между нотами; касание экранной клавиатуры тоже засчитывается; «Проверка пианино» останавливает упражнение; выдернуть кабель во время упражнения — продолжается; авиарежим — ноты рисуются
