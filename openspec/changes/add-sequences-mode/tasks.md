# Tasks

## 1. Логика (`engine/sequences`)

- [x] 1.1 `settings.ts`: `SequenceSettings`, значения по умолчанию, `readSequenceSettings` (D3) + тесты (по умолчанию, битые поля, границы счётчиков); проверка: `npm test` зелёный
- [x] 1.2 `generate.ts`: таблица диапазонов, `buildSequence`, `buildSession` (D1) + тесты «Аккорды в шаге» (1000 последовательностей), «Оба ключа», длина 3–8, только белые клавиши; проверка: `npm test` зелёный
- [x] 1.3 `session.ts`: автомат `start/played/tick/skip/next/stop/repeat` и `summarize` (D2) + тесты «Верная нота», «Верный аккорд», «Неверная нота», «Неполный аккорд», «Повторная ошибка», «Пропустить», «Ожидание», «Далее вручную», «Автоматический переход» (3000 мс), «Итог», «Всё пропущено», «Повторить», «Стоп во время отсчёта», время реакции с ошибками; проверка: `npm test` зелёный

## 2. Меню и хранение

- [x] 2.1 `engine/modes`: режим «Последовательности» первым, черновик настроек в автомате меню (`chooseMode` с подтверждёнными, `editDraft`, `confirm` с настройками), тесты обновлены: «Назад» отбрасывает черновик, «Выбрать» возвращает настройки (D3); проверка: `npm test` зелёный
- [x] 2.2 `storage/settings.ts`: `modeSettings.sequences` с `readSequenceSettings` + тесты (старые данные, битые данные, перезапуск); проверка: `npm test` зелёный

## 3. Интерфейс

- [x] 3.1 `ui/controls`: `ChoiceGroup`, `Stepper`, `Toggle` (D4); экран режима в `ModeMenu` показывает настройки «Последовательностей» из черновика; проверка: headless — «Настройки по умолчанию», «Граница счётчика», «Назад» не сохраняет, «Выбрать» сохраняет после перезагрузки; скриншот; цели ≥ 56 px
- [x] 3.2 `ui/sequences/SequenceStaff` (D5, D8): шаги без штилей, текущий (цвет + черта), верный (кольцо), пропущенный (квадрат), ошибка (пульсация / пунктир при reduced motion), полые головки правее; токен `--note-current`; проверка: headless-скриншоты всех состояний, 8 шагов × 3 ноты «Весь стан» на 1280×800 помещаются, контраст токенов ≥ 7:1 (скрипт), reduced motion через `emulateMedia`
- [x] 3.3 `ui/sequences/SessionSummary` (D6) и `useSequenceSession` (D7); проверка: `npm run build`
- [x] 3.4 `Keyboard`: центрирование по `focus` + `centerStartOn` в `engine/keyboard/layout` с тестом (D7); проверка: `npm test`, headless — «Басовая позиция» (видны C3–G3)
- [x] 3.5 `App` и `WaitingScreen`: запуск активного режима, слот «Проверка пианино / Пропустить / Далее (N)», итог на месте стана, остановка по «Стоп» и «Режим», закрытие итога при меню и «Проверке» (D7); CLAUDE.md §10 этап 4 — ссылка на C-STF-2 и `add-sequences-mode`; проверка: `npm run build`, `npm run lint`

## 4. Интеграция

- [x] 4.1 Headless-сценарии режима касанием экранной клавиатуры: старт; верно / мимо (полая головка) / пропуск; «Далее»; автоматический переход — замер 3 с ±100 мс; итог с числами; «Повторить» — те же ноты; «Новая»; «Стоп» во время отсчёта; меню во время сессии; строка кнопок не меняет число кнопок; e2e прошлых этапов (клавиатура, устройства, «Разминка», меню) проходят
- [x] 4.2 `npm run build`, `npm run lint`, `npm test` без ошибок; в описании PR — чек-лист для планшета: меню — «Последовательности» первыми, настройки и их запоминание; последовательность видна крупно, текущий шаг заметен боковым зрением; верно на пианино — шаг зелёный с кольцом; мимо — пульсация и красная полая нота; «Пропустить»; «Далее» и автоматический отсчёт; итог; «Повторить» / «Новая»; аккорды (нот в шаге 2–3) — не бывает ли ложных «неполных аккордов»; клавиатура центрируется на басовой позиции
