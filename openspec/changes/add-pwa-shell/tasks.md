# Tasks

## 1. Состояния связи в `midi/`

- [x] 1.1 Расширить `ConnectionState` в `src/midi/types.ts` до `unsupported | permission-denied | connecting | no-device | connected | lost` (D5) и обновить комментарии к каждому состоянию; проверка: `npm run build` без ошибок типов
- [x] 1.2 В `src/midi/midiAccess.ts` различать `unsupported` (нет API) и `permission-denied` (отказ `requestMIDIAccess`); проверка: в headless Chromium без MIDI и с подменённым API, который отклоняет запрос, экран показывает два разных состояния
- [x] 1.3 Добавить флаг «устройство уже было в этой сессии» и состояние `lost` при исчезновении всех входов; проверка: подменённый API — подключить вход, убрать, вызвать `statechange` → `lost`, вернуть → `connected`
- [x] 1.4 Вернуть из `startMidiMonitor` объект `{ stop, retry }`; `retry()` повторяет запрос доступа без перезагрузки; проверка: подменённый API сначала отклоняет, затем разрешает — после `retry()` состояние `connected`
- [x] 1.5 Ресинхронизация при `visibilitychange → visible` без повторного запроса доступа (D6), с отпиской в `stop()`; проверка: headless-скрипт подменяет список входов без `statechange`, эмулирует смену видимости — экран отражает новый список

## 2. PWA-оболочка

- [x] 2.1 Добавить `vite-plugin-pwa` в devDependencies и подключить в `vite.config.ts`: `registerType: 'prompt'`, `generateSW`, манифест по D3; проверка: `npm run build` создаёт `dist/manifest.webmanifest` и `dist/sw.js`, в манифесте `scope` и `start_url` = `/piaono_app/`
- [x] 2.2 Нарисовать `public/pwa-icon.svg` в палитре приложения и сгенерировать иконки `npx @vite-pwa/assets-generator@1 --preset minimal-2023 public/pwa-icon.svg`, подключить их в манифест и `index.html` (`apple-touch-icon`, `theme-color`); проверка: файлы есть в `dist/`, maskable-иконка корректно обрезается в DevTools → Application → Manifest
- [x] 2.3 Добавить типы `vite-plugin-pwa/react` в `tsconfig.app.json`; проверка: импорт `virtual:pwa-register/react` компилируется
- [x] 2.4 Проверить офлайн: `npm run preview`, в headless Chromium открыть страницу, дождаться активации SW, включить offline-режим и перезагрузить; проверка: страница открывается и показывает экран ожидания
- [x] 2.5 Дополнить README разделом «Установка на планшет и обновления»; проверка: описанные шаги совпадают с реальным поведением из 2.4

## 3. Экраны

- [x] 3.1 Поднять подписку на MIDI и журнал нот в `App` (D7), состояние `screen: 'waiting' | 'check'`; проверка: переключение экранов в headless-скрипте не вызывает повторного `requestMIDIAccess`
- [x] 3.2 Вынести текущий монитор в компонент `CheckScreen` («Проверка пианино») с кнопкой «Назад» ≥ 3.5rem; проверка: headless-скрипт — журнал нот сохраняется после ухода и возврата
- [x] 3.3 Сделать `WaitingScreen` с шестью состояниями, текстами и подсказками из `specs/connection-status` (D8); форма индикатора различается для каждого состояния; проверка: скриншоты всех шести состояний, контраст текста ≥ 7:1 (скрипт расчёта контраста, как в этапе 0), сравнение в оттенках серого
- [x] 3.4 Крупное имя последней ноты в состоянии «на связи»; проверка: подменённый note-on 60 → на экране «C4», размер самого крупного текста на экране
- [x] 3.5 Кнопка «Попробовать снова» в `permission-denied`, вызывающая `retry()`; проверка: сценарий из 1.4 проходит через интерфейс
- [x] 3.6 Кнопка «Обновить сейчас» по `needRefresh` из `useRegisterSW`; проверка: собрать две версии подряд в `preview`, после второй сборки кнопка появляется, нажатие перезагружает в новую версию
- [x] 3.7 Проверить раскладку на 1280×800 (ландшафт) и 390×844 (телефон): нет горизонтального скролла, нет скачков при смене состояний; проверка: скриншоты

## 4. Интеграция и проверка на железе

- [x] 4.1 `npm run build` и `npm run lint` без ошибок; удалить временные тестовые скрипты
- [ ] 4.2 В описании PR — чек-лист для планшета: установка на главный экран; запуск в авиарежиме; пианино не подключено → подключить; сон/пробуждение; выдернуть кабель → вставить; «Проверка пианино» и назад; обновление по кнопке после следующего деплоя
