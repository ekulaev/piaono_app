# Tasks

## 1. Логика (`engine/modes`)

- [x] 1.1 `modes.ts`: `ModeId`, `ModeInfo`, `MODES` (сейчас только «Разминка»), `DEFAULT_MODE`, `isModeId` (D1); проверка: `npm run build` без ошибок типов
- [x] 1.2 `modeMenu.ts`: автомат меню `openMenu/chooseMode/back/closeMenu/confirm` (D2) + тесты по сценариям «Открыть меню», «Экран „Разминки“», «Выбрать», «Назад», «Esc»/закрытие (ничего не меняет), «Недоступный режим не показывается»; проверка: `npm test` зелёный

## 2. Хранение

- [x] 2.1 `storage/settings.ts`: поле `activeMode` с проверкой по `MODES` (D3) + тесты «Перезапуск», «Повреждённые данные» (неизвестный режим, мусор), старые данные без поля; проверка: `npm test` зелёный

## 3. Интерфейс

- [x] 3.1 `ui/modes/ModeMenu.tsx` + CSS: подложка, панель справа, список с отметкой «выбран», экран режима с «Назад»/заголовком/«У этого режима нет настроек»/«Выбрать»/«Старт», «× Закрыть», Esc, фокус, без анимаций (D4); проверка: headless-скриншоты списка и экрана режима на 1280×800 и 740×360, контраст ≥ 7:1, все цели ≥ 56 px
- [x] 3.2 `App` и `WaitingScreen`: активный режим из настроек, кнопка «Режим: …» справа от «Старт», открытие меню останавливает упражнение, «Выбрать» сохраняет режим, «Старт» в меню запускает упражнение (D5); проверка: `npm run build`
- [x] 3.3 CLAUDE.md §10: меню режимов (C-MODE-1, `add-mode-menu`) между этапами 3 и 4; проверка: нумерация плана не сломана, текст непротиворечив

## 4. Интеграция

- [x] 4.1 Headless-проверка сценариев `mode-menu`: первый запуск «Режим: Разминка»; меню во время упражнения останавливает его; «Выбрать» закрывает без старта; «Старт» из меню запускает ноту; «Назад»; закрытие кнопкой, Esc и касанием вне меню (клавиша под меню не нажимается); выбор переживает перезагрузку; повреждённые данные → «Разминка»; строка кнопок на 740×360 без прокрутки страницы; e2e прошлых этапов проходят
- [x] 4.2 `npm run build`, `npm run lint`, `npm test` без ошибок; в описании PR — чек-лист для планшета: кнопка «Режим: Разминка»; меню открывается сразу, без анимации; во время упражнения кнопка режима останавливает его; «Выбрать» и «Старт» из меню; «Назад»; закрытие «×», касанием вне меню, клавиша под меню не играет; нажатия на пианино при открытом меню видны на клавиатуре; перезапуск приложения — режим сохранён
