# Tasks

## 1. Чистые функции `midi/`

- [x] 1.1 `src/midi/messages.ts`: `parseMidiMessage` (D2) + тесты по сценариям «События нажатия и отпускания» и «Неожиданные сообщения не ломают приём» (канал 10, velocity 0, педаль, active sensing, clock, sysex, обрезанное); проверка: `npm test` зелёный
- [x] 1.2 `src/midi/inputChoice.ts`: `chooseActiveInput` (D1) + тесты «Один вход», «Два входа без выбора», «Два входа с выбором», «Новый идентификатор после переподключения», пропажа выбранного; проверка: `npm test` зелёный

## 2. Монитор

- [x] 2.1 `midiAccess.ts`: один активный вход, `onConnectionChange(state, devices, activeId)`, `selectInput`, опция `preferred`, `held` и `releaseHeld()`, `open().catch()`, `try/catch` вокруг колбэка ноты (D3); типы в `types.ts`; проверка: `npm run build` без ошибок типов
- [x] 2.2 `midiAccess.test.ts` с подменённым Web MIDI (D6): все сценарии спеки `midi-input` — ноты только с активного входа, выбор, пропажа и возврат выбранного, отпускание зажатых нот при выдёргивании и смене входа, отказ `open()`, ошибка в обработчике, пересмотр при `visibilitychange`, состояния `connected/lost/permission-denied/unsupported`; проверка: `npm test` зелёный

## 3. Хранение и движок

- [x] 3.1 `storage/settings.ts`: поле `preferredInput` (D5) + тесты: старые данные без поля, корректное значение, битое значение; проверка: `npm test` зелёный
- [x] 3.2 Удалить `pianoReset` из `engine/keyboard` (тип, редьюсер, тест) (D4); проверка: `npm test` зелёный, `grep pianoReset src` пусто

## 4. Интерфейс

- [x] 4.1 `App`: настройки целиком в состоянии, `preferred` при старте монитора, `onSelectDevice` → `selectInput` + `saveSettings`, `activeId` в состоянии, вызов `pianoReset` убран; проверка: `npm run build`
- [x] 4.2 `CheckScreen`: устройства — кнопки ≥ 3.5rem, активное с заливкой и меткой «слушаю» (D7); проверка: headless-скрипт с двумя подменёнными входами — касание второго делает его активным, после перезагрузки выбор сохранён, ноты с первого в журнал не попадают; скриншот
- [x] 4.3 `WaitingScreen`: имя активного устройства и подсказка при нескольких устройствах (D7); проверка: headless-скриншот с одним и с двумя входами, контраст подсказки ≥ 7:1
- [x] 4.4 Уточнить этап 1 в CLAUDE.md §10: выбор устройства и тесты `midi/` — change `add-midi-device-selection`; проверка: текст раздела читается без противоречий

## 5. Интеграция

- [x] 5.1 `npm run build`, `npm run lint`, `npm test` без ошибок; headless-проверка клавиатуры из прошлого этапа не сломана (выдернутый кабель с зажатой клавишей отпускает её на экране)
- [ ] 5.2 В описании PR — чек-лист для планшета: одно пианино подключается само; сон/пробуждение — ноты идут без действий; кабель выдернуть с зажатой клавишей — клавиша на экране отпущена; при втором устройстве (если есть) — выбор на «Проверке пианино», запоминание после перезапуска
