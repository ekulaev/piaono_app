# Tasks

## 1. Логика (`engine/sequences`)

- [ ] 1.1 `anchors.ts`: `ANCHORS`, `anchorsIn`, `whiteIndex`, `intervalBetween`, `moveBy`, `MAX_INTERVAL`, `widestFitting`, `INTERVAL_NAMES` (D1) + тесты «Басовая позиция» (опорные C3 и F3), интервалы E4→C4 = ↓3, G4→A4 = ↑2, `widestFitting('position', 'octave')` = 5; проверка: `npm test` зелёный
- [ ] 1.2 `settings.ts`: поля `intervals` и `hints`, значения по умолчанию, `readSequenceSettings` (D8) + тесты «Настройки по умолчанию», «Старые сохранённые настройки», битые поля; проверка: `npm test` зелёный
- [ ] 1.3 `generate.ts`: `anchor` в `Sequence`, ход интервалами при `notesPerStep === 1`, прежний путь для аккордов (D2) + тесты «Ход интервалами» (1000 последовательностей на каждое сочетание ключа, диапазона и интервалов), «Аккорды в шаге», «Оба ключа» (`anchor === null` у аккордов); проверка: `npm test` зелёный
- [ ] 1.4 `hints.ts`: `initialVisibility`, `revealStep`, `classify`, `nextProgress`, `readHintProgress` (D3) + тесты «Уровень 1», «Две успешные подряд», «Трудная последовательность», «Ключи независимы», границы 0 и 3, «прочая» обнуляет счёт, повреждённые данные; проверка: `npm test` зелёный
- [ ] 1.5 `session.ts`: `hints` и `visibility` в состоянии, `revealStep` при ошибке (лишняя нота и неполный аккорд), `hinted` в `StepRecord`, пересчёт уровней при переходе в `finished`, `startSession(…, hints, now)`, `repeat` с текущими уровнями, `withoutHint` в `summarize` (D4) + тесты «Ошибка на первом шаге при уровне 0», «Пропуск» (подсказка не появляется), «Стоп посреди последовательности», «Без подсказки» (2 из 4), «Всё пропущено» («—»), сессия без подсказок (`hints: null` — уровни не меняются, `withoutHint: null`); прежние тесты сессии зелёные; проверка: `npm test` зелёный

## 2. Хранение

- [ ] 2.1 `storage/progress.ts`: ключ `piaono.progress.v1`, `loadHintProgress` / `saveHintProgress`; `browserStorage` вынесен в общий модуль `storage/` и используется `settings.ts` и `progress.ts` (D5) + тесты «Перезапуск» (сохранить → прочитать), «Повреждённые данные», недоступный `localStorage`; проверка: `npm test` зелёный

## 3. Интерфейс

- [ ] 3.1 `ui/controls`: проп `disabled` у `ChoiceGroup` и `Toggle` (пунктир, приглушённый текст, нажатия не действуют); `SequenceSettingsForm`: «Интервалы» после «Диапазона», «Подсказки» после «Нот в шаге», строки «В этом диапазоне — не больше …» и «Только для шагов из одной ноты» (D8); проверка: headless — «Настройки по умолчанию», «Интервал шире диапазона», «Аккорды отключают подсказки», запоминание после «Выбрать» и перезагрузки; цели ≥ 56 px; контраст недоступного текста ≥ 7:1
- [ ] 3.2 `staffDrawing.ts`: `staffGeometry(…, hintBand)`, `staveY` в геометрии, `makeStave` по `staveY` (D6); «Разминка» без полосы; проверка: `npm test`, headless-скриншот «Разминки» совпадает по положению стана с прежним
- [ ] 3.3 `sequenceDrawing.ts` и `SequenceStaff`: позиция якоря, полая головка якоря, подпись, цифры и стрелки-пути в полосе, позиции не зависят от видимости, `visibility` и `anchor` в ключе отрисовки (D7); проверка: headless-скриншоты уровней 3, 2, 1, 0 и ошибки на уровне 0 («Уровень 0 и ошибка» — координаты нот до и после совпадают); «Восемь шагов с якорем» на 1280×800 (промежуток ≥ 2 ширин головки); полоса ≤ 25 % зоны («Полоса подсказок»); высота цифры ≥ 2 промежутков стана; контраст подписей ≥ 7:1
- [ ] 3.4 `SessionSummary`: строка «Без подсказки: N из M» / «—», только если сессия шла с подсказками (D9); проверка: `npm run build`, headless — итог сессии с подсказками и без
- [ ] 3.5 `App` и `useSequenceSession`: подсказки действуют при `hints && notesPerStep === 1`, `loadHintProgress()` при старте, сохранение `state.hints` при изменении (D5); CLAUDE.md §10, этап 5 — ссылка на C-STF-3 и `add-interval-hints`; проверка: `npm run build`, `npm run lint`, headless — «Перезапуск» (уровень сохранился после перезагрузки страницы)

## 4. Интеграция

- [ ] 4.1 Headless-сценарии касанием экранной клавиатуры: старт с подсказками (якорь, подпись, «↑N/↓N»); «Нажатие ноты якоря» — ошибка; две успешные подряд → следующая последовательность без подписи якоря; трудная → подпись вернулась; «Подсказки выключены» и «Аккорды» — полосы нет, стан прежнего размера; «Повторить» — те же ноты и якорь; e2e прошлых этапов (клавиатура, устройства, «Разминка», меню, «Последовательности») проходят
- [ ] 4.2 `npm run build`, `npm run lint`, `npm test` без ошибок; в описании PR — чек-лист для планшета из C-STF-3, раздел 7: подсказки видны с расстояния игры и не путаются с аппликатурой; ноты читаются не хуже, чем без полосы; угасание ступенями после успешных последовательностей; подсказка после ошибки без сдвига нот; уровень сохранился после перезапуска; при «Нот в шаге» = 2 подсказки недоступны; «Позиция» + «До октавы» — строка «не больше квинты»; строка «Без подсказки» в итоге
